To run the chatbot , simply download the python file and the corpus(QA). Navigate from the cmd into the correct path that contains these 2 files and simply type python cw1.py to run it.

If you want to check the code of the cw1 python file , try to open it with notepad.